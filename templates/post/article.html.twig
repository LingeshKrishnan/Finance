{% extends 'base.html.twig' %}

{% block title %}Regiistered Redirection{% endblock %}

{% block body %}

<div class="container">
{% for message in app.Flashes('success') %}
<div class="alert alert-success">
    {{message}}

</div>
{% endfor %}
</div>
        <div class="container">
            <table class="table table-striped">

                <thead> 
                    <tr>
                    <td>Id</td>
                    <td>Name</td>
                   
                    <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    {% for articleform in articleform %}
                    <tr>
                        <td>{{article.id}}</td>
                        <td>
                        <a href="{{path('post.show',{id: post.id})}}">
                        {{articleform.name}}
                        </a>
                        </td>
                       
                        <td>
                        <a href="{{path('post.delete', {id: post.id})}}" class="text-danger">Delete</a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
{% endblock %}
